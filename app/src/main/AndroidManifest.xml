<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- TV: Déclarer que le touchscreen n'est PAS requis -->
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />
    <!-- TV: Support Leanback -->
    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />

    <application
        android:name=".NeoStreamApp"
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="NeoStream"
        android:supportsRtl="true"
        android:theme="@style/Theme.NeoStream"
        android:banner="@drawable/logo"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- Activity Mobile -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:theme="@style/Theme.NeoStream">
            <!-- Launcher Mobile -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Launcher TV (Leanback) -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- Video Player Activity -->
        <activity
            android:name=".ui.player.VideoPlayerActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:screenOrientation="landscape"
            android:theme="@style/Theme.NeoStream" />
    </application>
</manifest>
